{
  "installCommand": "pnpm install && cd dashboard && pnpm install",
  "buildCommand": "cd dashboard && pnpm run build",
  "outputDirectory": "dashboard/dist",
  "functions": {
    "api/events.ts": {
      "maxDuration": 60
    },
    "api/test-bot.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
